#### 7. Modify the `reminde2.c` program of Section 17.2 so that each element of the `reminders` array is a pointer to a `vstring` structure rather than a pointer to an ordinary string.

---

#### Answer: See `remind2.c`.
